// Unified auth approach example
// prisma/schema.prisma

model User {
  id       String   @id @default(cuid())
  name     String
  email    String   @unique
  password String
  role     UserRole @default(CUSTOMER)
  // ... other fields
}

enum UserRole {
  CUSTOMER
  ADMIN
  SUPER_ADMIN
}
